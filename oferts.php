<?php
require 'db.php';

// $searchTerm = isset($_GET['search']) ? '%' . trim($_GET['search']) . '%' : '%';
// $location = isset($_GET['location']) ? '%' . trim($_GET['location']) . '%' : '%';

// print_r($searchTerm);
print_r($_GET['search']);